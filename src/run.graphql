query RunInput {
  cart {
    lines {
      id
      quantity
      merchandise {
        __typename
        ...on ProductVariant {
          id
        }
      }
    }
  }
  discountNode {
    metafield(namespace: "volume-discount", key: "function-configuration") {
      value
    }
  }
}
