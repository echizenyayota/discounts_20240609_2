function restrictToServer(Component) {
  if (typeof window !== 'undefined') {
    const NoopComponent = () => null;
    NoopComponent.displayName = Component.displayName || Component.name;
    return NoopComponent;
  }
  return Component;
}

export { restrictToServer };
