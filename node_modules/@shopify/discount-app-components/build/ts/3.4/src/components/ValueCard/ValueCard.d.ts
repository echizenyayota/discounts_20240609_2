import React from 'react';
import { CurrencyCode } from '@shopify/react-i18n';
import { DiscountValueType, DiscountClass, PurchaseType, AppliesToEligibility } from '../../constants';
import { Field, ProductOrCollectionResource } from '../../types';
interface Props {
    fixedAmountDiscountValue: Field<string>;
    percentageDiscountValue: Field<string>;
    discountValueType: Field<DiscountValueType>;
    purchaseType: Field<PurchaseType>;
    oncePerOrder: Field<boolean>;
    discountClass: DiscountClass;
    currencyCode: CurrencyCode;
    sellsSubscriptions: boolean;
    isCodeDiscount: boolean;
    eligibility?: Field<AppliesToEligibility>;
    selectedItems?: Field<ProductOrCollectionResource[]>;
    productSelector?: React.ReactNode;
    collectionSelector?: React.ReactNode;
}
export declare function ValueCard({ fixedAmountDiscountValue, percentageDiscountValue, discountValueType, purchaseType, oncePerOrder, discountClass, currencyCode, sellsSubscriptions, isCodeDiscount, eligibility, selectedItems, productSelector, collectionSelector, }: Props): React.JSX.Element;
export {};
