import React from 'react';
import { Item } from './components';
interface Item {
    id: string;
    [key: string]: any;
}
export interface SelectedItemsListProps<T> {
    items: T[];
    renderItem(item: T): React.ReactNode;
    onRemoveItem?(id: string): void;
}
export declare function SelectedItemsList({ items, renderItem, onRemoveItem, }: SelectedItemsListProps<Item>): React.JSX.Element;
export {};
