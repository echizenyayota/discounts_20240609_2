import React from 'react';
import { DiscountClass } from '../../..';
interface Props {
    discountClass: DiscountClass;
    discountDescriptor: string;
}
export default function CombinationCardPattern({ discountClass, discountDescriptor, }: Props): React.JSX.Element;
export {};
