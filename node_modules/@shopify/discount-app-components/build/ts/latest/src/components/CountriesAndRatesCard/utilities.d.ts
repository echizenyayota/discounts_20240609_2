import type { REST_OF_WORLD, SupportedCountryCode as CountryCode } from '../../constants';
import type { Country } from '../../types';
/**
 * @returns a function that takes a country code or `REST_OF_WORLD` and returns a localized country object
 */
export declare function useLocalizeCountry(): (countryCode: CountryCode | typeof REST_OF_WORLD) => Country;
