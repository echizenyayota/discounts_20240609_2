import React from 'react';
export interface DiscountAppComponentsTestProviderProps {
    /**
     * Locale of the shop (e.g. `en-US`).
     */
    locale?: string;
    /**
     * The shop's time zone as defined by the IANA (e.g. `America/Los_Angeles`).
     */
    ianaTimezone?: string;
    /** Inner content of the application */
    children?: React.ReactNode;
}
export declare function DiscountAppComponentsTestProvider(props: DiscountAppComponentsTestProviderProps & {
    strict?: boolean;
}): React.JSX.Element;
