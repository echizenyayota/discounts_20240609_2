import { isFutureDate } from './is-future-date.esnext';
import { TimeUnit } from './constants/index.esnext';

function isLessThanOneWeekAgo(date, now = new Date()) {
  return !isFutureDate(date, now) && now.getTime() - date.getTime() < TimeUnit.Week;
}

export { isLessThanOneWeekAgo };
