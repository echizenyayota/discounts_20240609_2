import { isFutureDate } from './is-future-date.esnext';
import { TimeUnit } from './constants/index.esnext';

function isLessThanOneHourAgo(date, now = new Date()) {
  return !isFutureDate(date, now) && now.getTime() - date.getTime() < TimeUnit.Hour;
}

export { isLessThanOneHourAgo };
