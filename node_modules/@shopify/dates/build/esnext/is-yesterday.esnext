import { isSameDay } from './is-same-day.esnext';

function isYesterday(date, timeZone) {
  const now = new Date();
  const yesterday = new Date(now.valueOf() - 24 * 60 * 60 * 1000);
  return isSameDay(date, yesterday, timeZone);
}

export { isYesterday };
