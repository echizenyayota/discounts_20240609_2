'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var getDateTimeParts = require('./get-date-time-parts.js');
var isSameYear = require('./is-same-year.js');

function isSameMonth(date1, date2, timeZone) {
  const {
    month: month1
  } = getDateTimeParts.getDateTimeParts(date1, timeZone);
  const {
    month: month2
  } = getDateTimeParts.getDateTimeParts(date2, timeZone);
  return isSameYear.isSameYear(date1, date2, timeZone) && month1() === month2();
}

exports.isSameMonth = isSameMonth;
