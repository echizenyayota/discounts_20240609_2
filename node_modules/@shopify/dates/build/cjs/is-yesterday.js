'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var isSameDay = require('./is-same-day.js');

function isYesterday(date, timeZone) {
  const now = new Date();
  const yesterday = new Date(now.valueOf() - 24 * 60 * 60 * 1000);
  return isSameDay.isSameDay(date, yesterday, timeZone);
}

exports.isYesterday = isYesterday;
